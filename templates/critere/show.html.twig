{% extends 'base.html.twig' %}

{% block title %}Critere
{% endblock %}

{% block body %}
	<h1>Critere</h1>

	<table>
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ critere.id }}</td>
			</tr>
			<tr>
				<th>Label</th>
				<td>{{ critere.label }}</td>
			</tr>
			<tr>
				<th>Ptse1</th>
				<td>{{ critere.ptse1 }}</td>
			</tr>
			<tr>
				<th>Ptse2</th>
				<td>{{ critere.ptse2 }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_consigne_list_critere', {'id': consigne.id}) }}">Retour à la liste des critères</a>

	<a href="{{ path('app_critere_edit', {'id': critere.id }) }}">Modifier</a>

	{{ include('critere/_delete_form.html.twig') }}
{% endblock %}
