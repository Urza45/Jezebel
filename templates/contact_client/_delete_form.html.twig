<form method="post" action="{{ path('app_contact_client_delete', {'id': contact_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_client.id) }}">
    <button class="btn btn-danger" disabled="disabled">Supprimer ce contact</button>
</form>
