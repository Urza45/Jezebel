{% extends 'base.html.twig' %}

{% block title %}Norme
{% endblock %}

{% block body %}
	<section class="bg-secondary p-2 bg-opacity-25">
		<h1>Norme</h1>

		<table>
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ norme.id }}</td>
				</tr>
				<tr>
					<th>Label</th>
					<td>{{ norme.label }}</td>
				</tr>
				<tr>
					<th>Comments</th>
					<td>{{ norme.comments }}</td>
				</tr>
				<tr>
					<th>Picture</th>
					<td>{{ norme.picture }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_norme_index') }}">Retour à la liste des normes</a>

		<a href="{{ path('app_norme_edit', {'id': norme.id}) }}">Modifier</a>

		{{ include('norme/_delete_form.html.twig') }}
	</section>
{% endblock %}
